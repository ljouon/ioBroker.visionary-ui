export function SubComponent() {
    // const { stateObjects } = useVuiDataContext();
    const stateObjects = [];

    return (
        <>
            <div>{stateObjects.length}</div>
        </>
    );
}
